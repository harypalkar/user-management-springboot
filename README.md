This project is created to develop a Spring Boot-based RESTful API for a User Management System using Spring MVC and Maven. The application should
support basic CRUD operations for user accounts and include authentication simulation. This sample practice project
